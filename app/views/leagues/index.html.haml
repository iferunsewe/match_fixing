.league-container.font-override.text-center{:style =>"padding-top: 1%;"}
  %h1{:style =>"color:white;margin-bottom:20px;margin-top:0px;"} Leagues
  .league-table
    .table-responsive.font-override
      %table.table
        %thead
          %tr
            %th.text-center Name
            %th.text-center Location
            %th.text-center

        %tbody#items
          - @leagues.each do |league|
            %tr
              %td= link_to league.name, class: 'link-override-alt'
              %td= league.location
              %td
                = link_to 'Edit', edit_league_path(league), :class => "btn btn-xs btn-white nohoverwhite"
                = link_to 'Destroy', league, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => "btn btn-xs btn-white nohoverwhite"

  %br
  - if user_is_admin?
    %h4.text-center
      = link_to 'New League', new_league_path, class: "link-override"
